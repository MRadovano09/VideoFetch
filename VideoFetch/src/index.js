import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App' //this is a convention we're sticking to - creating a comp folder for all our components

ReactDOM.render(
    <App />,
    document.querySelector('#root')
)